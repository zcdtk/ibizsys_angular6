import { IBizFormItem } from './IBizFormItem';

/**
 * 表单IFrame部件
 * 
 * @export
 * @class IBizFormIFrame
 * @extends {IBizFormItem}
 */
export class IBizFormIFrame extends IBizFormItem {

    /**
     * Creates an instance of IBizFormIFrame.
     * 创建 IBizFormIFrame 实例
     * 
     * @param {*} [opts={}]
     * @memberof IBizFormIFrame
     */
    constructor(opts: any = {}) {
        super(opts);
    }
}
